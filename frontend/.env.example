# Backend API base URL. Leave empty to use relative paths with Vite proxy.
VITE_API_BASE_URL=

# Vite development proxy target (backend URL).
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Temporary actor UUID for development. Leave empty to use the setup screen.
VITE_ACTOR_USER_ID=
